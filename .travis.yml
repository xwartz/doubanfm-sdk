sudo: false
language: node_js
node_js:
  - "5"
  - "node"

cache:
  directories:
    - node_modules

install:
- npm install
- npm install -g codecov

script:
- npm run lint
- npm run build
- npm run test
- npm run cover
- codecov

os:
  - osx
